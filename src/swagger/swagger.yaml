openapi: 3.0.0
info:
  title: Patient Management System
  version: 1.0.0
servers:
  - url: /api
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas: {}
paths:
  # All module references
  /auth:
    $ref: './auth.yaml'
  /users:
    $ref: './users.yaml'
  /patients:
    $ref: './patients.yaml'
  /visits:
    $ref: './visits.yaml'
  /billing:
    $ref: './billing.yaml'
  /lab:
    $ref: './lab.yaml'
  /prescriptions:
    $ref: './prescriptions.yaml'
  /records:
    $ref: './medicalRecords.yaml'
  /maternity:
    $ref: './maternity.yaml'
  /dispenses:
    $ref: './dispenses.yaml'
  /triage:
    $ref: './triage.yaml'
  /unit:
    $ref: './units.yaml'
  /queue:
    $ref: './queue.yaml'
  /reports:
    $ref: './reports.yaml'
  /claim:
    $ref: './insurance.yaml'
  /payments:
    $ref: './payments.yaml'
  /admissions:
    $ref: './admissions.yaml'
  /beds:
    $ref: './beds.yaml'
  /discharges:
    $ref: './discharges.yaml'
  /audit:
    $ref: './audit.yaml'
security:
  - bearerAuth: []